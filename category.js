//CATEGORY

function goToAddCategory() {
    window.location.href = "add_category.html";
}

function goHome() {
    window.location.href = "user_dashboard.html"; //tukar
}

// Optional: simple arrow click (for now just alert)
function scrollLeft(type) {
    alert("Scroll left: " + type);
}

function scrollRight(type) {
    alert("Scroll right: " + type);
}
